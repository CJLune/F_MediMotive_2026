# German locale (`de/`)

| Path | Role |
|------|------|
| `index.html` | **Live** fallback at `https://medimotive.de/de/` (`noindex`) — visitors who click **DE** in the header |
| `_draft/index.html` | Full German homepage — review only, not deployed |
| `*.html` (other) | Translation work in progress — not deployed until go-live |

See `source-truth/de-launch-status.md` for the launch checklist.
